# 图书馆管理系统

一个功能完善的图书馆管理系统，包含图书管理、读者管理、借阅管理等功能。

## 功能特性

- 📊 **仪表盘**：数据概览和分类统计
- 📖 **图书管理**：添加、编辑、删除、搜索图书
- 👥 **读者管理**：管理读者信息
- 🔄 **借阅管理**：借书、还书、借阅记录查询
- 🎨 **现代化UI**：美观的用户界面，响应式设计

## 技术栈

- **前端**：HTML5, CSS3, JavaScript (原生)
- **后端**：Node.js + Express
- **数据库**：SQLite3

## 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 启动服务器

```bash
npm start
```

或使用开发模式（自动重启）：

```bash
npm run dev
```

### 3. 访问系统

打开浏览器访问：http://localhost:3000

## 部署到Cloud Studio

### 方法一：从GitHub导入

1. 访问 [Cloud Studio](https://cloudstudio.net/)
2. 登录并创建新工作空间
3. 选择「从GitHub导入」
4. 输入仓库地址：`https://github.com/likechiguoguo/library-management-system`
5. 等待工作空间创建完成
6. 在终端中运行：
   ```bash
   npm install
   npm start
   ```
7. 点击预览按钮访问应用

### 方法二：克隆仓库

```bash
git clone https://github.com/likechiguoguo/library-management-system.git
cd library-management-system
npm install
npm start
```

## 项目结构

```
library-management-system/
├── server.js           # 后端服务器
├── package.json        # 依赖配置
├── library.db         # SQLite数据库（运行后自动生成）
├── public/            # 前端文件
│   ├── index.html     # 主页面
│   ├── style.css      # 样式表
│   └── app.js         # 前端逻辑
└── README.md          # 说明文档
```

## 默认数据

系统启动后会自动创建示例数据：
- 5本示例图书
- 3个示例读者

## API接口

### 图书管理
- `GET /api/books` - 获取图书列表
- `GET /api/books/:id` - 获取单本图书
- `POST /api/books` - 添加图书
- `PUT /api/books/:id` - 更新图书
- `DELETE /api/books/:id` - 删除图书

### 读者管理
- `GET /api/readers` - 获取读者列表
- `POST /api/readers` - 添加读者
- `PUT /api/readers/:id` - 更新读者
- `DELETE /api/readers/:id` - 删除读者

### 借阅管理
- `GET /api/borrows` - 获取借阅记录
- `POST /api/borrows` - 借书
- `PUT /api/borrows/:id/return` - 还书

### 统计
- `GET /api/statistics` - 获取统计数据
- `GET /api/statistics/categories` - 获取分类统计

## 演示截图

系统包含四个主要模块：
1. **仪表盘** - 数据概览和图表统计
2. **图书管理** - 完整的CRUD操作
3. **读者管理** - 读者信息维护
4. **借阅管理** - 借还书流程

## 许可证

ISC
